defmodule Mix.Tasks.Saaskit.Gen.Admin do
  @shortdoc "Generates a context with functions around an Ecto schema"

  use Mix.Task
  import SaasKit.MixUtils

  @doc false
  def run(args) do
    args
  end
end
